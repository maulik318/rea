Prerequistis 
  AWS account 
  create sercret key to connect to instance
  create secure key for server access 
  Visual studio code
  terraform plugin


code execution sequance.
    no spsecific need 
    code should create 
        AWS VPC
        Subnet
        gateway
        ACLs
        nic card with ip address
        EC2 instance t2.micro ubuntu based on amazon aim
            ec2 instance will have nic will attach.
            run custom command on ubuntu bash during setup need to instrt inbetween two EOF line. 

 
